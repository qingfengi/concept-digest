# Concept Digest 审计与修复任务

## 已确认范围（必读）

用户要求审计未完成项目、修复后分别上传公有仓库；保留公开 URL，不发布个人密钥或私有运行状态。本轮只处理本目录，主任务负责最终安全扫描和上传。禁止读取私有 .env、账户配置或其他项目文件。

本轮目标：修复已确认的概念树并发覆盖，以及直接影响现有概念解释、保存、设置和导出流程的缺失文件或边界；不扩展学习产品功能。

## 问题与计划

| 编号 | 状态 | 问题 | 计划 |
| --- | --- | --- | --- |
| CD-001 | 已修复 | explainConcept 在网络等待前读取树，并发请求覆盖彼此的新节点 | 模型请求并行，存储读改写排队，提交前重新读取 |
| CD-002 | 已修复 | 删除父节点期间迟到的解释可能恢复已删除数据或留下孤儿节点 | 所有写操作统一排队，提交前验证父节点仍存在 |
| CD-003 | 已修复 | manifest 引用的 sidepanel、options、content 文件全部缺失 | 补齐现有声明对应的最小可用界面与通信，删除无需要的 content.css 引用 |
| CD-004 | 已修复 | 上游失败正文可能包含凭据；URL /v1 可能重复 | 不回传上游原文，验证端点并标准化请求路径；模型请求拒绝重定向 |
| CD-005 | 已修复 | AI JSON 结构和消息来源未严格验证 | 验证响应对象、概念列表和扩展内部消息边界 |
| CD-006 | 已修复 | 内容脚本可读密钥；设置接口直接返回密钥 | storage 仅可信上下文可读，设置接口返回掩码，网页消息仅允许选词 |
| CD-007 | 已修复 | 环状历史可能无限遍历；存储失败阻断后续写入 | 遍历使用已访问集合，失败操作释放队列且不伪装成功 |

## 验证与发布

- [x] 使用 Node 内置测试模拟 chrome.storage 与 fetch，不访问真实模型。
- [x] 覆盖并发新增、删除交错、失败队列恢复、父节点删除、输入和响应错误。
- [x] 检查 manifest 与两个 HTML 的全部资源引用存在。
- [x] `npm run check` 全部通过；`npm test` 11/11 通过。
- [x] 发布源码凭据模式扫描仅命中测试中的 `synthetic-private-value` 合成占位值，无个人密钥。
- [x] 独立 Edge 152 测试配置加载扩展，设置→解释→递归→保存→刷新读取→导出→网页选词→删除分支通过；410px 无横向溢出，截图已查看。
- [x] 实际模拟接口返回 HTTP 503：错误显示、输入保留、不新增节点、不回显上游正文；随后删除操作正常。
- [ ] 公开源码扫描和独立上传。

原目录没有测试或 README。本轮新增任务记录、说明和测试，无新增运行依赖。

## 源码发布白名单

`background.js`、`manifest.json`、`sidepanel.html`、`sidepanel.js`、`options.html`、`options.js`、`content.js`、`panel.css`、`package.json`、`.gitignore`、`README.md`、`TASK.md`、`test/background.test.cjs`、`scripts/verify-ui.cjs`、`icons/icon16.png`、`icons/icon48.png`、`icons/icon128.png`。

## 仍需主任务验收

未安装到已有用户浏览器，未使用真实模型账户。实测为独立浏览器内的扩展页面标签页，不宣称工具栏打开的原生侧栏已经验收。当前密钥是浏览器本机存储而非系统保险库，README 与设置页已明确说明。当前不自动保存失败概念输入到磁盘，不承诺浏览器强制关闭后的恢复。后台选词只在当前服务工作线程中暂存，浏览器回收后台线程后应重新选词。

本轮优先使用已有 Edge，没有新增运行依赖；最初尝试下载 Chromium，确认 Edge 可用后取消下载，残留只在被排除的 D: `qa/` 中。
